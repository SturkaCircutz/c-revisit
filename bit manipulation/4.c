/*
Given int nums[] and int n, return the largest value 
of nums[i] ^ nums[j] over all pairs. Values fit in 32 bits, 
n can be up to 200000, so the O(n squared) loop is too slow.

int findMaximumXOR(int* nums, int numsSize);





*/